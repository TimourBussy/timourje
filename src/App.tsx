import { SiteName } from "./components/ui/SiteName";

export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-tr from-[#1e173b] to-[#1d1a3a]">
      <SiteName variant="header" />
    </div>
  );
}